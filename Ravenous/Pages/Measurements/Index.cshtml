@page
@model Ravenous.Pages.Measurements.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Measurement[0].MeasurementName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Measurement) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MeasurementName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PkMeasurement">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PkMeasurement">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PkMeasurement">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
