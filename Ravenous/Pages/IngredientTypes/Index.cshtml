@page
@model Ravenous.Pages.IngredientTypes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IngredientType[0].IngredientTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.IngredientType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IngredientTypeName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PkIngredientType">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PkIngredientType">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PkIngredientType">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
